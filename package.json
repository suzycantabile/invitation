{
  "name": "invitation",
  "version": "1.0.0",
  "description": "A simple, responsive, easy-to-edit wedding website template.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "gh-deploy": "git push origin :gh-pages && git subtree push --prefix dist origin gh-pages"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "firebase": "^9.18.0"
  },
  "devDependencies": {
    "webpack-cli": "^5.0.1"
  }
}
